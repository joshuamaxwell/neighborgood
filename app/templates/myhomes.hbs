<div class="container">
	<div class="jumbo-wrapper">
		<h1>My Homes</h1>
		<ul>
		{{#each}}
		  {{#link-to 'property' this}}
		    <li>{{address}}</li>
		  {{/link-to}}
		{{/each}}
		</ul>
	</div>
</div>

