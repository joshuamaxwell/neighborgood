<div class="app">
    {{!-- this div.app is the component that will always be present so it includes the top nav bar --}}
    <nav>
        <div class="text-center">
           <a class="open-menu nav-icon" href="#drawer"><span class="glyphicon glyphicon-th-list"></span></a>
           <input class="input-standard">
           <a href="#" class="nav-icon"><span class="glyphicon glyphicon-filter"></span></a>
        </div>
    </nav>
    <div class="spacer"></div>
   {{outlet}}
</div>

{{!-- the navigation drawer is below --}}
<nav id="drawer">
   <ul class="list">
      <li>{{#link-to 'index'}}Home{{/link-to}}</li>
      <li>{{#link-to 'login'}}Log in{{/link-to}}</li>
      <li>{{#link-to 'profile'}}Profile{{/link-to}}</li>
   </ul>
</nav>