<div class="app">
    {{!-- this div.app is the component that will always be present so it includes the top nav bar --}}
    <nav class="navigation-bar">
        <div class="text-center">
           <a class="open-menu nav-icon" href="#drawer"><span class="glyphicon glyphicon-th-list"></span></a>
           <form {{action "lookupAddress" searchParams target='controllers.properties' on="submit"}}>
             {{input class="input-standard" placeholder="Find a home" value=searchParams}}
             {{#link-to 'properties' class="nav-icon home-icon"}}<span {{action 'lookupAddress' searchParams target='controllers.properties'}} class="glyphicon glyphicon-home"></span>
             {{/link-to}}
           </form>  
           <a href="#" class="nav-icon filter-icon"><span class="glyphicon glyphicon-filter"></span></a>
        </div>
    </nav>
    <div class="spacer"></div>
   {{outlet}}
</div>

{{!-- the navigation drawer is below --}}
<nav id="drawer">
   <ul class="list">
      <li>{{#link-to 'index'}}Home{{/link-to}}</li>
      <li>{{#link-to 'properties'}}Search Results{{/link-to}}</li>
      <li>{{#link-to 'myhomes'}}My Homes{{/link-to}}</li>
      <li>{{#link-to 'account'}}Account{{/link-to}}</li>
      <li>{{#link-to 'style-guide'}}Style Guide{{/link-to}}</li>
   </ul>
</nav>